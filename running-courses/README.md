# 러닝 코스 데이터

"어디 뛰지" 앱에 넣을 코스 원고와 이미지. **`data/*.json`이 유일한 원본**이고 나머지는 전부 여기서 생성된다.

```bash
python3 build.py              # 전체 다시 생성
python3 build.py gangwon      # data/gangwon.json 만
```

## 산출물

| 경로 | 내용 |
|---|---|
| `data/*.json` | **원본.** 여기만 고친다 |
| `courses.json` | 앱에 넣을 병합본 (42개 코스) |
| `<도시>.md` | 사람이 읽는 문서 (24개 도시) |
| `images/<id>_hero.png` | 1125×900 — `.dhero` 375×300 @3x |
| `images/<id>_thumb.png` | 156×156 — `.rth` 52×52 @3x, radius 11pt |
| `images/<id>-poiN_thumb.png` | 경유지 썸네일 |

## 코스 하나의 구조

`05-data.md`의 `Course` 모델을 따른다.

```json
{
  "id": "seoul-banpo-10k",
  "n": "반포 한강 10km 왕복",
  "km": 10.0, "min": 58, "lv": "보통", "mood": "야경",
  "tags": ["초보 추천", "야경", "호수·강변"],
  "scene": "river", "time": "night",
  "src": "러너 후기",
  "photo": "https://tong.visitkorea.or.kr/...",
  "photoTitle": "반포한강공원",
  "photoLicense": "한국관광공사 · 공공누리 제1유형",
  "headline": "...", "subhead": "...",
  "body": [], "deep": [], "ops": [], "unsure": [],
  "poi": [{ "n": "...", "d": "...", "photo": "..." }]
}
```

- `body` → 화면의 **어떤 길인지**
- `deep` → **더 알아두면 좋은 것**
- `ops` → **가기 전에 알아두면 좋아요**
- `unsure` → **아직 확인 중인 것**. 확인 못 한 항목을 지어내지 않고 여기 담는다
- `poi` → **지나는 곳**

## 이미지

사진을 쓰지 않고 **직접 생성한다.** `05-data.md`에 기존 이미지가 Wikimedia CC BY-SA라 출시 전 촬영자 확인이 필요하다고 적혀 있었는데, 생성 이미지는 그 문제가 없다.

만드는 방식은 이렇다. `photo`에 적힌 한국관광공사 사진에서 색을 뽑아, 그 팔레트로 플랫 일러스트를 그린다. 실제 장소의 색감은 남고 저작권 문제는 없어진다.

**씬은 6종** — `05-data.md`의 지도 도형 어휘와 같은 값을 쓴다.

| 씬 | 성격 | 지도 도형 |
|---|---|---|
| `lake` | 호수 | 닫힌 폐곡선 |
| `city` | 도심 공원 | 닫힌 폐곡선 |
| `river` | 하천·강변 | 왕복 |
| `coast` | 해안 | 왕복 |
| `trail` | 숲길 | 편도, 구불구불 |
| `hill` | 업힐 | 편도, 지그재그 |

`time`은 `day` / `sunset` / `night`. 비워두면 원본 사진 밝기로 자동 판별한다. 밤은 별과 창문 불빛이 들어간다.

같은 씬끼리 똑같아 보이지 않도록 `id` 해시로 구도 변형 3종과 수평선 높이가 갈린다.

## 문구 규칙

`06-copy.md`를 따른다. **"검증·데이터·확보·실측·열림"은 화면에 쓰지 않는다.**
확인 못 한 정보는 숨기지 말고 `unsure`에 적는다.

## 지금 담긴 범위

24개 도시 · 42개 코스.

| 권역 | 도시 |
|---|---|
| 수도권 | 서울 6 · 인천 2 · 수원 2 · 고양 1 · 성남 1 |
| 강원 | 춘천 2 · 강릉 2 · 속초 1 |
| 충청 | 대전 2 · 세종 1 · 청주 1 · 천안 1 |
| 전라 | 전주 2 · 광주 1 · 목포 1 · 여수 1 · 순천 1 |
| 경상 | 대구 2 · 창원 2 · 울산 1 · 포항 1 · 경주 1 |
| 부산 | 부산 5 |
| 제주 | 제주 2 |

## 아직 안 한 것

- 좌표. `polyline`과 체크포인트 `coordinate`가 비어 있어 지금은 전부 `routed: false` 상태로 봐야 한다
- 체크포인트 이야기 원고
- 트레일 전용 코스(북한산·관악산·무등산)는 도시 코스에 흡수돼 있고 따로 분리하지 않았다

## 코스 데이터가 아닌 문서

- [10_러닝크루_SNS.md](10_러닝크루_SNS.md) — 지역별 크루 계정, 해시태그 검색법
- [11_체크리스트.md](11_체크리스트.md) — 안전·계절·편의시설 공통 팁
